# Copy to .env in this folder (scripts/score-sync/.env)
# Use the service_role key — NOT the anon key from VITE_SUPABASE_ANON_KEY

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-secret
