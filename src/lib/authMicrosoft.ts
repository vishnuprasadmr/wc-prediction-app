/**
 * Microsoft / Azure AD auth — DISABLED for now.
 * Re-enable when the app is registered in the company Azure tenant (Groupe Astek).
 * See authGoogle.ts for the active OAuth provider.
 */

/*
import { LEAGUE_ID, LEAGUE_INVITE_CODE, supabase } from './supabase'
import type { User } from '@supabase/supabase-js'
import {
  ALLOWED_EMAIL_DOMAIN,
  AUTH_ERROR_KEY,
  consumeAuthError,
  consumeOAuthRedirectError,
  displayNameFromUser,
  isAllowedWorkEmail,
  isRecentlyCreated,
  setAuthError,
} from './authOAuth'

// ... full Microsoft implementation preserved in git history
*/

export {}
